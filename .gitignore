# Ambiente virtual Python
venv/
__pycache__/
*.pyc

# Segredos e configuração local
.env

# Base de dados (contém dados de clientes — nunca em repositório público)
database/*.db
database/*.sqlite3

# Ficheiros de sistema operativo
.DS_Store

# Editor
.vscode/

# Notas
docs/COMANDOS.md

# Documentação de trabalho — só backup local, nunca no repositório público
# (o único ficheiro de docs/ que vai para o repo real é o relatório Word final, ex: docs/*.docx)
docs/*.md
